import React from 'react'
import { ListingType } from '../pages/Listing'

const ListingItem = ({key, listing}:{key:string, listing:ListingType}) => {
  return (
    <div>ListingItem</div>
  )
}

export default ListingItem